import { CarouselCover } from './carousel-cover';

describe('CarouselCover', () => {
  it('should create an instance', () => {
    const directive = new CarouselCover();
    expect(directive).toBeTruthy();
  });
});
